// PDF generation placeholder — currently the report is sent as a rich HTML email.
// If server-side PDF generation is needed later, add pdfkit as a dependency
// and implement the generation logic here.
//
// Install: npm install pdfkit @types/pdfkit --legacy-peer-deps
